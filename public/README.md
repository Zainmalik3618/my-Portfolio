Place your public assets in this folder.

Profile photo:

- Save your picture as `profile.jpg`
- Put it directly inside this `public` folder
- The Hero section will use it from `/profile.jpg`

Resume:

- Save your resume as `resume.pdf`
- Put it directly inside this `public` folder
- The Download Resume button will use it from `/resume.pdf`
